
import React from 'react'

export default function winningPage(props) {
    return (
        <div>
            
            <h1>Win!!!!!!! {props.playerNumber}</h1>
            <button>play again?</button>
        </div>
    )
}
